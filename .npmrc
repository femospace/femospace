# Allow npm to skip platform-incompatible optional packages silently.
# This prevents errors like EBADPLATFORM for lightningcss-win32-x64-msvc
# when building on Linux (Vercel, GitHub Actions, etc.)
optional=true
legacy-peer-deps=false
ignore-scripts=true
