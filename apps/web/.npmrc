# Prevent platform-specific optional packages (e.g., lightningcss-win32-x64-msvc)
# from causing build failures on Linux (Vercel/CI). These are optional native binaries
# that only apply to specific OS/CPU combinations and should be silently skipped.
optional=true

